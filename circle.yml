machine:
  environment:
    PYENV_VERSION: 3.6.3:2.7.14
    TOXENV: py36,py27
  post:
    - cd /opt/circleci/.pyenv && git pull
    - for i in ${PYENV_VERSION//:/ } ; do pyenv install -s $i ; done
dependencies:
  pre:
    - echo 'a'
  override:
    - echo 'b'
  post:
    - echo 'c'
test:
  override:
    - tox -v
  post:
    - codecov
